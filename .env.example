# Nextcloud Passwords MCP server configuration.
# Copy to `.env` for local development (never commit the real file).

# Base URL of your Nextcloud instance (no trailing slash). Must be https://.
NEXTCLOUD_URL=https://cloud.example.com

# Nextcloud username.
NEXTCLOUD_USER=your-username

# Exact reference to a dedicated Nextcloud app-password in macOS Keychain or
# Linux Secret Service. The value itself must never be placed in this file.
NEXTCLOUD_CREDENTIAL_SERVICE=nc-passwords-mcp:cloud.example.com
NEXTCLOUD_CREDENTIAL_ACCOUNT=your-username

# Optional: print debug logs to stderr (never logs secrets). Default: off.
# DEBUG=1
