{
  "name": "passwords-mcp",
  "version": "0.3.0",
  "private": true,
  "type": "module",
  "description": "Metadata-only MCP server and credential-store helper for Nextcloud Passwords",
  "license": "MIT",
  "engines": {
    "node": ">=24"
  },
  "bin": {
    "passwords-mcp": "./dist/index.js",
    "passwords-mcp-keychain": "./dist/keychain-cli.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "dev": "tsx watch src/index.ts",
    "start": "node dist/index.js",
    "lint": "eslint src",
    "typecheck": "tsc --noEmit",
    "test": "node --test --import tsx 'src/**/*.test.ts'",
    "prepublishOnly": "tsc",
    "pack:tarball": "tsc && npm pack"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "eslint": "^10.0.0",
    "tsx": "^4.0.0",
    "typescript": "^5.5.0",
    "typescript-eslint": "^8.0.0"
  },
  "packageManager": "pnpm@9.15.9"
}
